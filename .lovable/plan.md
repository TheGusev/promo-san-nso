

# Замена фоновых фото в Hero на главной

## Что делаем
Заменяем два ротирующихся фоновых изображения в `Hero.tsx` на новые загруженные фото:
- Фото 1 — дезинфекция в офисе (сотрудник в защитном костюме обрабатывает офисное помещение)
- Фото 2 — обработка участка (сотрудник обрабатывает сад/участок у частного дома)

## Файлы

### Перезаписать (используются в `Hero.tsx`)
1. **`public/images/hero-bg-1.png`** — заменить на `user-uploads://877b99d4-ee86-4633-bc93-a2d399660c85.png` (офис)
2. **`public/images/hero-bg-2.png`** — заменить на `user-uploads://a5b3719a-6c65-4904-9123-787419190f9a.png` (участок)

### Не трогаем
- `src/components/Hero.tsx` — ссылки на `/images/hero-bg-1.png` и `/images/hero-bg-2.png` уже корректные, ротация и crossfade работают как есть
- Тёмный оверлей `bg-black/55` остаётся для читаемости текста

## Итог
Две команды копирования файлов. Хедер сразу подхватит новые фото, ничего больше менять не нужно.

