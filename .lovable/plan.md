## Что меняем

### 1. Убрать затемнение Hero на лендингах (`src/pages/lp/LandingPage.tsx`)
- Удалить оба затемняющих оверлея (`bg-background/85 backdrop-blur-[2px]` и `bg-gradient-to-r from-background/95 via-background/80 to-background/30`).
- Чтобы текст оставался читаемым на фото без затемнения:
  - На заголовок/подзаголовок добавить лёгкий `drop-shadow` (`[text-shadow:_0_2px_8px_rgba(0,0,0,0.5)]`) — это не затемняет фото, только усиливает контраст текста.
  - Чипсу-бейдж «Гарантия…» — фон `bg-background/80 backdrop-blur-sm` (только под чипсой, не на всей секции).
  - Чипсы фактов внизу (Выезд / Договор / Без запаха) — обернуть в общий контейнер `bg-background/70 backdrop-blur-sm rounded-lg p-3`.
  - Карточка с формой и ценой уже непрозрачная — не трогаем.
- Фото остаётся видимым целиком, без серой плёнки сверху.

### 2. Заменить «1–2 ч/часа» на «в течение часа» в клиентских CTA
Только в Hero и шагах работы лендингов — там, где это обещание SLA. FAQ-ответы оставляем как есть (там «срочный выезд 1–2 часа» — корректное уточнение).

- `src/pages/lp/LandingPage.tsx`:
  - стр. 127: `Выезд за 1–2 ч` → `Выезд в течение часа`
  - стр. 202: `В день обращения, 1–2 часа` → `В день обращения, в течение часа`

Глобальные FAQ/блоги/serviceAreas/responseTime не трогаем — это другая семантика (зональное время, экспозиция препарата).

### 3. Обновить инфо безопасности на /lp/uchastok (`src/data/landingContent.ts`)
- Поле `safetyNote` для `uchastok` (стр. 232–233) заменить на:
  `"Безопасно для детей и животных. Используем сертифицированные акарициды (Цифокс, Сипаз, Бриз). Эффект сохраняется до 2–3 месяцев. Не повреждает газон, цветы и плодовые деревья при правильном применении."`
- В блоке `included` (стр. 229) `"Срок защиты — 4–6 недель в зависимости от погоды"` → `"Срок защиты — до 2–3 месяцев в зависимости от погоды"`.
- В FAQ uchastok (стр. 256–257) ответ «На сколько хватает одной обработки?» обновить: `"От клещей — до 2–3 месяцев в зависимости от количества дождей. От комаров — 3–4 недели..."`.

## Что НЕ трогаем
- Фото Hero, импорты, layout, формы, аналитика, schema.org.
- Цены, гарантию, остальные лендинги (klopy, tarakany) — текст безопасности там корректен.
- FAQ/services/area data вне `landingContent.ts`.
