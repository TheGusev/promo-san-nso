## Правки виджета плавающих контактов и темы

### 1. Ссылка MAX
В `src/data/siteConfig.ts` (`SITE_CONFIG.links.max`) поменять на:
`https://max.ru/u/f9LHodD0cOLiUyWa_VfefOTALeW3UZQoPjKtoCWCRfLLAk0QxvTZkWtg2Kc`

### 2. Плавающий виджет `src/components/FloatingContact.tsx`
- Удалить верхнюю зелёную кнопку «Обратный звонок» (`handleCallback` + иконка `PhoneCall`) из раскрытого списка.
- Убрать импорт `PhoneCall`, аналитику `callback_click` из этого компонента не трогаем (может использоваться в других местах).
- Добавить автосворачивание через 5 сек бездействия:
  - `useEffect` при `isOpen=true` ставит `setTimeout(()=>setIsOpen(false), 5000)`.
  - Таймер сбрасывается при движении мыши/тапе внутри виджета (`onMouseMove`, `onTouchStart` на контейнере) и очищается на `unmount`/закрытии.
- Поведение главной синей кнопки внизу:
  - Первый тап — раскрывает меню (как сейчас).
  - Повторный тап при открытом меню — **звонит** (`tel:${SITE_CONFIG.phoneClean}` + `trackGoal('phone_click')`), а не сворачивает. Меню при этом закрывается.
  - Иконка `X` при открытом состоянии больше не нужна — оставим `Phone`, чтобы визуально подсказывало «нажми ещё раз — позвонить». Крестика не будет; сворачивание — по таймеру или клику вне виджета (уже реализовано).

### 3. Зелёный цвет в тёмной теме
Проблема: `--secondary` (зелёный `#4CAF50` / hsl вариант) в dark-теме теряет насыщенность из-за низкой контрастности с тёмным фоном.

В `src/index.css` в блоке `.dark`:
- Поднять яркость `--secondary` до более сочного оттенка (`142 70% 55%` вместо текущего приглушённого), `--secondary-foreground` оставить тёмным для контраста на кнопках.
- Проверить, что зелёные галочки в Hero и иконки FAQ (используют `text-secondary` / `bg-secondary`) стали чётче.

Изменения — только визуальные токены, компоненты не трогаем.

### Файлы
- `src/data/siteConfig.ts`
- `src/components/FloatingContact.tsx`
- `src/index.css`
